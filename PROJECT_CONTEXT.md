# PROJECT_CONTEXT

## 1. 文档目的

本文档用于统一沉淀当前项目的真实状态、代码核查结论、上线判断以及后续执行清单，作为后续开发和上线推进的共同上下文。

更新时间：2026-03-26

---

## 2. 项目概况

项目名称：AI 投资决策助手

项目定位：
- 面向 A 股普通投资者的 AI 决策辅助系统
- 不直接给出买卖指令
- 通过结构化分析、风险提示、用户画像适配、行为干预与复盘机制，帮助用户减少决策错误

当前仓库结构：

```text
F:\investment
├── design\                    # 产品设计文档
├── structure\                 # 数据库与结构设计
├── investment-front\          # 前端应用（React + TypeScript + Vite）
├── investment-back\           # 后端服务（FastAPI + SQLAlchemy + PostgreSQL）
├── PROJECT_SUMMARY.md
├── PROJECT_README.md
├── IMPLEMENTATION_PLAN.md
├── DEPLOYMENT_GUIDE.md
├── start.bat
└── start.sh
```

技术栈：
- 前端：React 19、TypeScript、React Router、Tailwind CSS、Vite
- 后端：FastAPI、SQLAlchemy 2.0、Pydantic 2、PostgreSQL、Alembic、Structlog

---

## 3. 已核查的项目现实情况

基于对仓库结构、关键代码、启动方式、接口、构建结果和部署脚本的核查，当前项目更接近：

`可演示 / 可内测的 MVP`

而不是：

`可直接正式上线的生产版本`

### 3.1 已经具备的能力

- 前端不是纯静态页面，主流程已经接入后端 API
- 已有登录页、首页、用户画像、股票搜索、单股分析、交易前自检、交易后复盘、记录页、复盘任务页
- 后端已具备真实股票搜索和实时/近期市场数据抓取能力
- 后端分析任务具备异步处理雏形
- 前端类型检查通过
- 前端生产构建通过
- 后端代码基本可导入与编译

### 3.2 已验证结果

验证结论：
- 前端 `npm run lint` 通过
- 前端 `npm run build` 通过
- 后端 `python -m compileall main.py src` 通过
- 后端 `pytest` 未形成有效回归保障

实际观察到的问题：
- `pytest` 过程中没有有效测试资产
- 后端虚拟环境中 `email-validator` 缺失，说明环境和依赖安装并不稳定
- 仓库中不存在成体系的 `tests` 目录

---

## 4. 当前项目功能状态判断

### 4.1 前端状态

前端已经接入真实 API 的页面包括：
- 股票搜索
- 分析创建
- 分析结果轮询
- 用户画像读取/更新
- 分析记录
- 复盘任务

当前前端的主要现实问题：
- 部分页面文案存在乱码
- 观察列表和关注理由仍然主要依赖本地 `localStorage`
- 文档与真实启动方式不一致

### 4.2 后端状态

后端已经具备：
- 用户、画像、分析、观察列表、复盘任务等基础模型
- 股票搜索接口
- 股票详情抓取
- 分析创建与异步处理
- JWT 登录接口基础能力

当前后端的主要现实问题：
- 认证流程仍带有明显开发态逻辑
- 生产安全边界不足
- 外部数据依赖存在稳定性风险
- Docker 构建链路与实际代码结构不一致
- 测试缺失

### 4.3 AI 能力状态

当前“分析结果生成”仍以规则模板和固定逻辑为主，不是真正成熟的 Agent / LLM 生产链路。

说明：
- 项目配置中存在 AI 相关字段
- 但分析生成服务主体仍是规则生成文本和结构化结论
- 因此对外表述更适合使用“规则辅助分析 / 模板化决策卡片”，不宜夸大为成熟大模型分析系统

---

## 5. 真实上线判断

建议的当前判断：

- 可以准备做 `内测上线`
- 不建议直接做 `正式公测上线`

建议收口后的 v1 核心功能范围：
- 登录
- 用户画像
- 股票搜索
- 单股分析
- 交易前自检
- 交易后复盘
- 历史记录
- 复盘任务

观察列表建议：
- 要么在 P1 完成服务端持久化后再上线
- 要么 v1 暂时弱化或隐藏，避免半成品影响体验

---

## 6. 核心风险与阻塞项

以下问题属于上线前必须正视的阻塞项：

### 6.1 文案与编码问题

- 前端 UI 存在明显乱码
- 后端部分提示文案存在乱码
- 部分文档也是乱码状态

影响：
- 直接影响产品可用性
- 影响用户信任感
- 增加联调和验收成本

### 6.2 观察列表未真正闭环

现状：
- 前端观察列表和关注理由仍主要存本地
- 后端其实已有 watchlist API

判断：
- 这是“前后端未闭环”的典型问题
- 对正式上线不友好，跨设备和换浏览器后数据不可持续

### 6.3 认证仍为开发态

现状：
- `DEBUG=True`
- 无 token 时可回退测试用户
- 启动时自动创建 `testuser / testpassword123`

判断：
- 这套逻辑可以用于本地开发
- 不能直接进入正式生产路径

### 6.4 配置与安全边界不足

现状：
- 默认 `SECRET_KEY` 未收紧
- 关键环境配置存在硬编码倾向
- 配置校验不够严格

说明：
- 本轮用户已明确：数据库设置和 Agent API Key 不修改
- 但这不改变这些项在生产中属于风险点的事实

### 6.5 启动与部署口径不一致

现状：
- 前端 `npm run dev` 实际走 `server.ts`
- 文档中又多处按 Vite 默认口径描述
- `start.bat`、`start.sh`、README、部署文档之间描述不统一

影响：
- 团队接手成本高
- 部署与联调容易踩坑

### 6.6 Docker 构建链路不可信

现状：
- Dockerfile 依赖 `pyproject.toml` / `poetry.lock`
- 实际仓库以 `requirements.txt` 为主

影响：
- 现有 Docker 构建不可视为可靠生产交付链路

### 6.7 测试体系缺失

现状：
- 没有成体系测试目录
- 没有关键接口回归测试
- 没有前端主流程自动化验证

影响：
- 每次修改都会提高回归风险
- 上线后定位问题成本高

---

## 7. 本轮执行约束

用户明确约束：

- 不修改数据库设置
- 不修改 Agent API Key

因此本轮后续规划默认：
- 不调整数据库连接配置
- 不变更已有 Agent / AI key 值
- 规划重点放在代码闭环、上线可用性、工程化和质量保障

---

## 8. 上线阶段划分

建议目标分为两个阶段：

### 阶段 A：可内测上线

目标：
- 主链路能跑通
- 文案可用
- 登录和基础分析可稳定使用
- 核心接口可观测

### 阶段 B：可正式上线

目标：
- 主流程全闭环
- 自动化测试覆盖关键路径
- 部署链路稳定
- 具备基本监控、回滚和验收机制

---

## 9. P0 / P1 / P2 执行清单

以下清单是在“不改数据库设置、不改 Agent API Key”前提下制定。

### P0：基础修复与上线前最低可用版本

- [ ] 修复全项目乱码和中文文案统一，覆盖前端页面、后端报错、日志展示和文档
- [ ] 统一启动方式和端口口径，明确开发态和生产态分别使用哪个入口
- [ ] 修正文档和脚本不一致的问题
- [ ] 修复 Docker 构建链路，使其与当前 `requirements.txt` 方案一致
- [ ] 关闭生产路径上的 debug 回退逻辑，确保正式环境不会自动放行测试用户
- [ ] 冻结 v1 上线范围，只保留核心主流程功能
- [ ] 完成一轮手工 smoke checklist，至少跑通：
  - 登录
  - 搜索股票
  - 创建分析
  - 结果轮询
  - 记录页查看
  - 复盘任务查看
  - 用户画像编辑
- [ ] 同步更新项目文档，确保 README、项目总结、部署文档描述一致

P0 目标结果：
- 项目可作为一个“能演示、可内测”的版本稳定运行

### P1：主链路闭环与上线必备能力

- [ ] 将观察列表和关注理由改成服务端持久化，替换当前 `localStorage` 方案
- [ ] 完成 watchlist 与 focus reason 的前后端闭环
- [ ] 把登录认证从“开发可用”补到“上线可用”
- [ ] 强化 token 失效处理、未登录访问拦截和会话恢复
- [ ] 加强外部行情/公告接口的容错、缓存、超时、降级和错误提示
- [ ] 统一 API 错误返回与前端错误展示
- [ ] 补基础运维能力：
  - 健康检查
  - 启动失败日志
  - 关键接口失败可追踪
  - 发布后故障可快速定位

P1 目标结果：
- 项目具备稳定内测能力
- 主业务流程基本闭环

### P2：质量保障与发布准备

- [ ] 建立后端测试基线，至少覆盖：
  - 登录
  - 用户画像
  - 股票搜索
  - 创建分析
  - 查询结果
  - 观察列表
  - 复盘任务
- [ ] 建立前端关键流程 E2E 或集成测试，至少覆盖：
  - 登录
  - 搜索
  - 分析
  - 结果页
  - 用户画像
  - 历史记录
- [ ] 建立 staging 环境验收流程
- [ ] 沉淀发布清单：
  - 回滚步骤
  - 数据备份步骤
  - 日志查看入口
  - 故障排查顺序
- [ ] 补最小监控项：
  - 接口错误率
  - 分析成功率
  - 第三方数据源失败率
  - 前端 401 / 白屏比例
- [ ] 完成上线前整体验收

P2 目标结果：
- 项目具备正式上线前的最小质量保障能力

---

## 10. 推荐执行顺序

建议按以下顺序推进：

1. 先完成 P0
2. 再进入 P1
3. P1 主链路稳定后补 P2
4. P2 验收通过后再考虑正式发布

不建议压缩的事项：
- 乱码修复
- debug 认证关闭
- 启动链路统一
- Docker 修正
- 观察列表服务端化

---

## 11. 时间预估

按单人推进估算：

- 到“可内测上线”：约 2 到 3 周
- 到“可正式上线”：约 4 到 6 周

说明：
- 若中途继续扩需求，周期会延长
- 若先收口功能范围，周期可控

---

## 12. 结论

当前项目不是从零开始，已经具备继续推进上线的良好基础：
- 前后端骨架完整
- 主功能页面较全
- 前端可构建
- 后端已有实际数据抓取能力

但同样也不能把现状误判成“已经只差部署”：
- 仍有乱码问题
- 仍有认证开发态逻辑
- 仍有前后端未闭环能力
- 仍缺少可靠部署链路
- 仍缺少测试基线

因此最合理的推进方式是：

`以 P0 修基础、以 P1 补闭环、以 P2 做保障，再进入正式上线。`

